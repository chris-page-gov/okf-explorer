/** Catalogue size and work per invocation are deliberately different bounds.
 * A third, explicitly scoped engine does not authorise a third replay attempt.
 */
export const ENGINE_CATALOGUE_LIMIT = 3;
export const COMPATIBLE_REPLAY_LIMIT = 2;
