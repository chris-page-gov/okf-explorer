name: Evaluation Foundry nightly full shadow

on:
  schedule:
    - cron: "41 1 * * *"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  adversarial-gate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
      - uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: "3.12"
      - run: python3 -m pip install -r requirements-okf.txt
      - name: Reject all reconstructed late-finding classes
        run: >-
          python3 scripts/check_heritage_adversarial.py
          --output "$RUNNER_TEMP/adversarial-results.json"
      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7
        with:
          name: nightly-adversarial-gate-results
          path: ${{ runner.temp }}/adversarial-results.json

  browser-three-engine:
    needs: adversarial-gate
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          fetch-depth: 0
      - uses: pnpm/action-setup@0ebf47130e4866e96fce0953f49152a61190b271 # v6
        with:
          version: 10
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: 24
          cache: pnpm
          cache-dependency-path: apps/okf-explorer/pnpm-lock.yaml
      - uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: "3.12"
      - working-directory: apps/okf-explorer
        run: pnpm install --frozen-lockfile
      - name: Materialize SvelteKit TypeScript configuration
        working-directory: apps/okf-explorer
        run: pnpm exec svelte-kit sync
      - name: Install Site dependencies
        run: python3 -m pip install -r requirements-okf.txt
      - name: Validate and deterministically build Explorer
        working-directory: apps/okf-explorer
        run: |
          pnpm check
          pnpm test
          pnpm sbom:check
          pnpm build:determinism
      - name: Assemble Site for rendered Foundry pages
        run: python3 scripts/build_site.py
      - name: Install Firefox and WebKit browser dependencies
        working-directory: apps/okf-explorer
        run: pnpm exec playwright install --with-deps firefox webkit
      - name: Run full Chrome, Firefox and WebKit terminal assurance
        working-directory: apps/okf-explorer
        run: pnpm test:e2e:terminal

  foundry-full-family:
    needs: adversarial-gate
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          fetch-depth: 0
      - uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: "3.12"
      - run: python3 -m pip install -r requirements-okf.txt
      - name: Run full Foundry and candidate checks
        run: |
          python3 scripts/check_evaluation_foundry.py
          python3 scripts/retarget_heritage_source_snapshots.py --check
          python3 scripts/build_heritage_evaluation.py --check --fixture all
          python3 scripts/export_publication_unit.py --descriptor publication-units/heritage-coventry-warwickshire/publication-unit.json --check
          python3 -m unittest discover -s tests
