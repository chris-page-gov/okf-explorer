import fs from 'node:fs';import path from 'node:path';import crypto from 'node:crypto';
const [explorer,dwp,out]=process.argv.slice(2).map(p=>path.resolve(p));
if(!explorer||!dwp||!out)throw Error('Usage: node scripts/package_dwp_learning_demo.mjs BUILT_EXPLORER_ROOT DWP_ROOT NEW_OUTPUT_DIRECTORY');
if(fs.existsSync(out))throw Error('Output directory already exists; retain it and choose a new candidate directory.');
fs.mkdirSync(out,{recursive:true});
for(const p of ['_app','explore','favicon.svg','okf-explorer-build-manifest.json','okf-registry.json'])fs.cpSync(explorer+'/_site/'+p,out+'/'+p,{recursive:true});
for(const p of ['combined','logical-context'])fs.cpSync(dwp+'/'+p,out+'/dwp/'+p,{recursive:true});
fs.cpSync(dwp+'/docs/learning',out+'/guide',{recursive:true});
fs.cpSync(dwp+'/docs/logical-evidence-units.md',out+'/guide/logical-evidence-units.md');
fs.copyFileSync(dwp+'/NOTICE.md',out+'/DWP-NOTICE.md');fs.copyFileSync(dwp+'/LICENSE',out+'/DWP-LICENSE');
fs.copyFileSync(explorer+'/LICENSE-CODE.md',out+'/EXPLORER-LICENSE-CODE.md');fs.copyFileSync(explorer+'/LICENSE.md',out+'/EXPLORER-LICENSE.md');
fs.writeFileSync(out+'/index.html',`<!doctype html><html lang="en-GB"><meta charset="utf-8"><meta name="viewport" content="width=device-width"><title>DWP learning demonstration</title><link rel="icon" href="favicon.svg"><style>body{font:19px/1.6 system-ui;margin:3rem auto;padding:0 1rem;max-width:65ch}a{color:#005ea5}a:focus{background:#ffdd00;color:black}li{margin:1rem 0}</style><h1>DWP learning demonstration</h1><p>12 paths · 112 activities · 40 supplied question entries.</p><p>Independent experimental teaching material. Not official DWP training, benefits advice or an entitlement decision.</p><ol><li><a href="explore/?bundle=%2Fdwp%2Fcombined%2Fokf-explorer.json#overview">Open the learning programme</a></li><li><a href="explore/?bundle=%2Fdwp%2Flogical-context%2Fokf-explorer.json#overview">Inspect the published retrieval-unit corpus</a></li><li><a href="guide/README.md">Read the presenter script and assessment setup</a></li></ol><p>The learning programme uses the combined Reader's existing evidence anchors. The retrieval-unit corpus remains separately inspectable, with complete spans and dependencies. No source or interpretation acquires extra authority by appearing in a lesson.</p><p>Use fictional examples. Assessment decisions require a configured facilitator; a practice tick cannot award a pass.</p></html>`);
fs.writeFileSync(out+'/serve.mjs',`import http from 'node:http';import fs from 'node:fs';import path from 'node:path';import {fileURLToPath} from 'node:url';
const root=path.dirname(fileURLToPath(import.meta.url));const port=Number(process.env.PORT||8130);
const types={'.html':'text/html; charset=utf-8','.js':'text/javascript','.css':'text/css','.json':'application/json','.gz':'application/gzip','.svg':'image/svg+xml','.md':'text/plain; charset=utf-8','.yamlld':'text/plain; charset=utf-8'};
http.createServer((req,res)=>{try{const rel=decodeURIComponent(new URL(req.url,'http://localhost').pathname);let file=path.resolve(root,'.'+rel);if(!file.startsWith(root+path.sep)&&file!==root){res.writeHead(403).end();return;}if(fs.statSync(file).isDirectory())file=path.join(file,'index.html');res.setHeader('Content-Type',types[path.extname(file)]||'application/octet-stream');res.setHeader('Cache-Control','no-store');fs.createReadStream(file).on('error',()=>res.destroy()).pipe(res);}catch{res.writeHead(404).end('Not found');}}).listen(port,'127.0.0.1',()=>console.log('Open http://127.0.0.1:'+port));
`);
fs.writeFileSync(out+'/START-HERE.md',`# DWP learning demonstration\n\nRequires Node.js 22.18 or later. Unzip the folder, open a terminal in it and run:\n\n\`\`\`sh\nnode serve.mjs\n\`\`\`\n\nOpen http://127.0.0.1:8130 in Chrome. Stop the server with Ctrl+C. Do not open the HTML using file://; the Reader needs HTTP to verify and fetch bounded shards. The server binds only to this computer.\n\nSee guide/README.md for the ten-minute presenter script. Keep the full folder together. Official source links require an internet connection; local corpus records and lesson navigation work from the package. There is no private assessor key, real claimant data, claim-submission service or entitlement calculator in this package.\n\nThe private facilitator key for the author's local rehearsal remains outside the shared package. A recipient can browse, practise and export submissions; to assess independently, configure their own public roster in the producer and rebuild.\n`);
const files=[];function walk(dir){for(const name of fs.readdirSync(dir).sort()){const p=path.join(dir,name);if(fs.statSync(p).isDirectory())walk(p);else if(name!=='PACKAGE-MANIFEST.json'){const b=fs.readFileSync(p);files.push({path:path.relative(out,p),bytes:b.length,sha256:crypto.createHash('sha256').update(b).digest('hex')});}}}walk(out);
fs.writeFileSync(out+'/PACKAGE-MANIFEST.json',JSON.stringify({schema:'okf-learning-demo-package.v1',bundle_snapshot:JSON.parse(fs.readFileSync(dwp+'/combined/okf-explorer.json')).snapshot,files},null,2)+'\n');console.log(out,files.length);
