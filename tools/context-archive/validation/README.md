# Local retained-example observations

The final current-exporter journey is
[local-attempt-07/observation.json](local-attempt-07/observation.json).
Chrome 153.0.8010.52 ran at 07:11 UTC on 21 September 2026: seven focused checks,
58 loopback responses, no browser console/page errors, no public or model calls.
Its four-example export contained 236 files and 2,793,182 bytes. The retained
artifact manifest SHA-256 is
`292c9f2e907c8a804db3a6f12ae495ff615175af05c0d2eb7b2959ce37b9f5ee`.

The examples are a genuine synthetic two-record/one-relationship museum case,
the retained local current care-home package, the unknown-term empty control,
and the retained historical care-home package. The DWP packages retain their
original complete hashes and insufficient status. The historical original
engine remains unknown in the archive declaration.

The harness checked keyboard skip-link focus, exact synthetic Unicode and
inert markup, separate source/capture dates, selected counts, directed
relationships, whole-package reconstruction, a 390px mobile layout and a
deliberately corrupted descriptor. It hid prior evidence on corruption. This
is targeted automated verification, not a complete accessibility or public
service assessment. Screenshots were visually inspected for mobile layout.

## Preserved attempts

| Attempt | Outcome and scope |
| --- | --- |
| 01 | Loopback listen was denied by the sandbox before browser launch. The original harness did not catch the server error. Its executed source, output inventory and separate admission-failure note are retained. |
| 02–04 | Browser attempts failed before descriptor retrieval. Focusing the skip link changed document flow; the subsequent pointer target moved during the click. Attempt 04 retained the focused anchor and unchanged URL. |
| 05 | After fixing the skip-link layout and the initially empty synthetic fixture, the harness compared the previous example's record count with the newly clicked case before its loading finished. It failed; the wait was corrected to require the new case heading. |
| 06 | Seven checks passed. Subsequent schema admission hardening changed the exporter source binding, so this pass is preserved separately and not relabelled as the final candidate. |
| 07 | Final source-bound pass, with all reviewed schema, budget and file-admission corrections present. |

Earlier attempt directories preserve the executed harness, observed results
where the harness reached its error handler, screenshots where captured, and
generated file/source hash inventories. They do not contain every earlier
uncommitted exporter source or the temporary generated archive. Do not claim
that an old attempt can be reconstructed with the current producer. The final
producer and retained source-package inputs support a new deterministic export;
run it into a fresh directory rather than rewriting these observations.

The initial command also encountered a missing output-parent directory before
creating an observation. That parent-directory admission error is distinct
from an executed browser journey.

Reproduction commands and the local-only harness requirements are in the
[tool guide](../README.md). Eleven focused offline tests pass, including the
independent review's negative controls for rehashed inconsistent budgets,
prototype-named fields, unknown schema branches and a file-to-FIFO admission
race. Successful byte integrity does not close evidence or specialist gates.
